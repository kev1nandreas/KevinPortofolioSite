const { createApp } = Vue

createApp({
    data() {
        return {
            navBar: false,
            maxHeight1: true,
            maxHeight2: true,
            maxHeight3: true,
            maxHeight4: true,
            maxHeight5: true,
            maxHeight6: true,
            maxHeight7: true,

        }
    },
    computed: {
        
    },
}).mount('#app');